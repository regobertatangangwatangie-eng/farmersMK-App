server:
  port: 8089

spring:
  datasource:
    url: jdbc:h2:mem:orangemoney_db
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true