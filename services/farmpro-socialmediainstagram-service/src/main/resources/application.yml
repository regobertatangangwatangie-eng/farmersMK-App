server:
  port: 8090

spring:
  datasource:
    url: jdbc:h2:mem:instagramdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true