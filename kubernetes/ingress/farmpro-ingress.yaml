apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: farmpro-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: api.farmpro.example.com
      http:
        paths:
          - path: /sample-service(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: farmpro-sample-service
                port:
                  number: 80
