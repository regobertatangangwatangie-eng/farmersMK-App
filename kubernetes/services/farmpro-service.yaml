apiVersion: v1
kind: Service
metadata:
  name: farmpro-sample-service
spec:
  selector:
    app: farmpro-sample-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
