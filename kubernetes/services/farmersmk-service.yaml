apiVersion: v1
kind: Service
metadata:
  name: FarmersMK-sample-service
spec:
  selector:
    app: FarmersMK-sample-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
