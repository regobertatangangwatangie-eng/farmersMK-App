server:
  port: 8084

spring:
  application:
    name: wallet-service

  datasource:
    url: jdbc:h2:mem:wallet_db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true